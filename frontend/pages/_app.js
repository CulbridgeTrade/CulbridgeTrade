import '../CulbridgeSubmissionForm';
import '../CulbridgeAdminDashboard';
import '../CulbridgeExporterDashboard';
import '../frontend/lib/api';

export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
